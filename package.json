{
  "name": "drawio-software-icons",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "sync": "node scripts/sync.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "node --test tests/*.test.mjs && npm run test:ui && uv run --no-project python -m unittest discover -s collections/alibaba-cloud/tests",
    "test:ui": "tsx --test tests/react-ui.test.tsx",
    "typecheck": "tsc --noEmit",
    "build": "npm run validate && npm run typecheck && node scripts/generate.mjs && vite build && node scripts/check-distribution.mjs && node scripts/publish-targets.mjs && node scripts/check-unified.mjs && node --test collections/alibaba-cloud/tests/test_plugin.cjs",
    "dev": "node scripts/generate.mjs && vite --host 127.0.0.1",
    "preview": "vite preview --host 127.0.0.1",
    "build:cloudflare": "npm run build && node scripts/build-worker-index.mjs && node scripts/prepare-legacy-pages.mjs && node scripts/check-cloudflare.mjs",
    "dev:cloudflare": "wrangler dev",
    "check:cloudflare": "wrangler deploy --dry-run --outdir .worker-build/bundle",
    "deploy:cloudflare": "wrangler deploy",
    "deploy:cloudflare:production": "wrangler deploy --env production"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^5.2.0",
    "fast-xml-parser": "^5.0.0",
    "fflate": "^0.8.2",
    "tsx": "^4.23.15",
    "typescript": "^5.9.0",
    "vite": "^7.0.0",
    "wrangler": "^4.141.0"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^1.48.0",
    "radix-ui": "^1.6.7",
    "react": "^19.3.0",
    "react-dom": "^19.3.0",
    "tailwind-merge": "^3.7.0",
    "tailwindcss": "^4.3.3"
  }
}
